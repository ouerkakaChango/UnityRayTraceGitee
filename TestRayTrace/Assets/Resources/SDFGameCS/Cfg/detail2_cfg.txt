valMaps:
SceneName Detail2
BackGroundColor float3(0,0,0)

Blocks:

###BLOCK MainRender
if (minHit.bHit)
{ 
	re = RenderSceneObj(ray, minHit);
	SetIndirectColor(re, ray, minHit);
	re *= RenderSceneSDFShadow(ray, minHit);
}
else
{
	re = (@BackGroundColor);
	//re = GetEnvIrradiance_equirectangular(envBgTex, ray.dir, true);
}
###BLOCK
