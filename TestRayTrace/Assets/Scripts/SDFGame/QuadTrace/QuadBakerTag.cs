using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum QuadColorType
{
    texture,
}
public class QuadBakerTag : MonoBehaviour
{
    public QuadColorType colorType = QuadColorType.texture;
    public TexSysTag texTag;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
